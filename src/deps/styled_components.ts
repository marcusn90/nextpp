import * as styledComponents from "https://dev.jspm.io/styled-components@4.4.1";

const sc: any = styledComponents.default;
export default sc.default;
const {
  css,
  ServerStyleSheet,
} = sc;

export { css, ServerStyleSheet };
